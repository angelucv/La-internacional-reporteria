# Promoción de datos al BI

Registrar aquí cada vez que un archivo pase del trabajo interno / `Reportes` / `Info` al proyecto **`La-Internacional-BI`** (`data/public/`).

| Fecha | Archivo en BI | Origen (carpeta / REQ) | Notas |
|-------|----------------|-------------------------|-------|
| | | | |

Ruta del proyecto BI: `../La-Internacional-BI`.
